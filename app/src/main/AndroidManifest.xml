<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.labour.homie">

    <application
        android:allowBackup="true"
        android:icon="@drawable/h"
        android:label="@string/app_name"
        android:roundIcon="@drawable/h"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Signupforuser"></activity>
        <activity android:name=".Signupforlabour" />
        <activity android:name=".BaseActivity" />
        <activity android:name=".LabourProfile" />
        <activity android:name=".ListOfLabours" />
        <activity
            android:name=".UserPage"
            android:label="@string/title_activity_user_page"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>